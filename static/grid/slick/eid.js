// Generated by CoffeeScript 1.6.3
(function() {
  var EID;

  window.Sicki || (window.Sicki = {});

  window.Sicki.EID = EID = (function() {
    function EID() {}

    EID.prototype.getAllEids = function() {
      return $.ajax({
        url: '../../../eid/events',
        async: true,
        dataType: 'json'
      });
    };

    EID.prototype.makeCol = function(id, name, width) {
      if (width == null) {
        width = 100;
      }
      if (!name) {
        name = id.substring(0, 1).toUpperCase() + id.substring(1, id.length);
        name = name.replace(/_/g, ' ');
      }
      return {
        id: id,
        name: name,
        field: id,
        width: width,
        editor: Slick.Editors.Text
      };
    };

    EID.prototype.getColumns = function() {
      var colIds, cols, id;
      colIds = ['event_name', 'disease', 'host', 'location', 'eid_id', 'start_date'];
      return cols = (function() {
        var _i, _len, _results;
        _results = [];
        for (_i = 0, _len = colIds.length; _i < _len; _i++) {
          id = colIds[_i];
          _results.push(this.makeCol(id));
        }
        return _results;
      }).call(this);
    };

    return EID;

  })();

}).call(this);
